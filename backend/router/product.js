const express=require('express')
const product=express.Router();

product.get('/',)

product.get('/:id',)

product.post('/',)

product.patch('/:id',)

product.delete('/:id')


exports={
    product
}
